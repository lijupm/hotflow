{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-info">
    <div class="panel-heading">
        <h3 class="panel-title"><b>Operatingroom creation</b></h3>
    </div>
    <div class="panel-body">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" /> <a class="btn btn-info" href="{{ path('operating_rooms_index') }}">Back to the list</a>
    {{ form_end(form) }}
    <div>

    </div>
    </div>
    </div>
{% endblock %}
